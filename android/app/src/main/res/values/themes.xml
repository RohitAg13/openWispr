<resources>
    <style name="Theme.OpenWispr" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Used by RewriteActivity / FixDictationActivity so they float over the host app. -->
    <style name="Theme.OpenWispr.Translucent" parent="Theme.OpenWispr">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <!-- Branded launch screen for the main activity (legacy fallback for API < 31). -->
    <style name="Theme.OpenWispr.Splash" parent="Theme.OpenWispr">
        <item name="android:windowBackground">@drawable/splash_window</item>
    </style>
</resources>
