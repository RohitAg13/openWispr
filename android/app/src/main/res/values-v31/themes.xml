<resources>
    <!-- Android 12+ system splash: orange background + animated mic icon. -->
    <style name="Theme.OpenWispr.Splash" parent="Theme.OpenWispr">
        <item name="android:windowSplashScreenBackground">@color/brand_orange</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/brand_orange</item>
        <item name="android:windowBackground">@color/warm_white</item>
    </style>
</resources>
